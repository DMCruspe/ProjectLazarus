// Этот код временно заглушен, пока не будет реализована аутентификация
// На данный момент он просто пропускает запросы
exports.requireAdmin = (req, res, next) => {
    // В будущем здесь будет проверка, является ли пользователь админом
    // if (req.user && req.user.role === 'admin') {
    //   next();
    // } else {
    //   res.status(403).json({ message: 'Доступ запрещён. Требуются права администратора.' });
    // }
    next();
};

exports.requireSuperadmin = (req, res, next) => {
    // В будущем здесь будет проверка, является ли пользователь суперадмином
    // if (req.user && req.user.role === 'superadmin') {
    //   next();
    // } else {
    //   res.status(403).json({ message: 'Доступ запрещён. Требуются права суперадминистратора.' });
    // }
    next();
};